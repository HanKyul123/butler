<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Hotel">
	<select id="Searchkeyword" parameterType="string" resultType="HotelDTO">
		select * from business_tb where business_name=#{keyword} 
		or business_hashtag=#{keyword}
		or business_addretc like('%${keyword}%')
	</select>
</mapper>
